function hello(name) {
  if (typeof name === 'undefined') {
    return 'Hello!';
  } else {
    return `Hello, ${name}!`;
  }
}

function hello(name) {
  if (typeof name === 'undefined') {
    return 'Hello!';
  } else {
    return `Hello, ${name}!`;
  }
}
